// This entry is used only by code emitted by the Vite instrumentation plugin. It is deliberately
// separate from the customer-facing SDK root, whose runtime surface is exclusively `gdp()`.
export { dispatchAutoTrack } from './runtime/autotrack-dispatch.js'
